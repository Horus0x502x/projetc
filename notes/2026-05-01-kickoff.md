# 2026-05-01 — Kickoff

## Contexte

Démarrage formel du projet RE sur IW4x / CoD MW2. Niveau utilisateur : débutant.
Stratégie : phases progressives ; à chaque phase, un livrable concret + un commit.

## État des lieux trouvé sur disque

### Préfixes Wine (Lutris)

- **`../CODA/`** : préfixe Wine actif, MW2 + IW4x installé, ~1.5 Go d'assets, crash récent
  (`minidumps/iw4x-r5039-20260501001438.dmp` — daté du jour).
- **`../Call of Duty Modern Warfare 2/`** : préfixe vide (skeleton uniquement).
  → **Décision** : ignoré pour l'instant.

### Binaires

| Fichier | Type | Taille | Note |
|---|---|---|---|
| `iw4mp.exe` | PE32 GUI x86 | 4.7 Mo | MW2 multi original |
| `iw4sp.exe` | PE32 GUI x86 | 3.5 Mo | MW2 solo original |
| `iw4x.exe` | PE32 console x86 | 3.9 Mo | client IW4x |
| `iw4x.dll` | PE32 DLL x86 | 3.6 Mo | core IW4x |
| `IW4Vanilla.exe` | PE32 console x86 | 3.9 Mo | variante "vanilla" |
| `iw4v.dll` | PE32 DLL x86 | 3.5 Mo | variante "vanilla" |
| `iw4v-plus.dll` | PE32 DLL x86, **12 sections** | 4.0 Mo | suspect — à investiguer en priorité (sections inhabituelles) |
| `iw4x-launcher` | **ELF x86-64 Linux, NON STRIPPÉ avec debug_info** | 25 Mo | excellente cible pour démarrer (symboles disponibles) |

### Ressources annexes

- **`../tools/mw2-raw-gsc/`** : scripts GSC du jeu en clair — `aitype/`, `character/`, `maps/`,
  `common_scripts/`, `animscripts/`, `codescripts/`. Pont logique gameplay ↔ binaire.

## Décisions prises

1. **Workspace** : `/home/horus/Bureau/G/projetc/`, structure 7-phases (cf. `METHODOLOGY.md`).
2. **Symlinks** vers les binaires plutôt que copies (économie disque, vue à jour).
3. **Git local d'abord**, push GitHub vers `Horus0x502x` plus tard.
4. **MW2 prefix vide** : ignoré.
5. **`iw4v-plus.dll`** : investigation prioritaire en phase 1 (12 sections = inhabituel).
6. **Objectif final** : non décidé, démarrage par la recon (commune aux 4 chemins A/B/C/D).

## Pièges techniques actés

- Tous les binaires PE sont **32-bit (i386)** → tooling 32-bit partout (Ghidra, Frida, scanmem).
- Sous Wine, ImageBase Windows ≠ adresse Linux (double ASLR).
- Le launcher Linux ELF est notre **point d'entrée pédagogique** (symboles, pas de couche Wine).

## Prochain pas

Phase 1 — recon statique des PE et de l'ELF launcher. Livrables :
`recon/01-pe-headers.md` et `recon/02-strings-triage.md`.
